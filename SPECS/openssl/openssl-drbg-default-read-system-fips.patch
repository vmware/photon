diff -rupN vmware-OpenSSL_1_0_2v-orig/crypto/o_init.c vmware-OpenSSL_1_0_2v-patched/crypto/o_init.c
--- vmware-OpenSSL_1_0_2v-orig/crypto/o_init.c	2020-05-05 14:06:00.000000000 +0000
+++ vmware-OpenSSL_1_0_2v-patched/crypto/o_init.c	2020-07-29 19:14:28.145021208 +0000
@@ -57,6 +57,7 @@
 #include <openssl/err.h>
 #ifdef OPENSSL_FIPS
 # include <openssl/fips.h>
+# include <openssl/fips_rand.h>
 # include <openssl/rand.h>
 
 # ifndef OPENSSL_NO_DEPRECATED
@@ -66,6 +67,51 @@ void FIPS_crypto_set_id_callback(unsigne
 #endif
 
 /*
+ *
+ * Enable FIPS mode based on host FIPS mode / env variable.
+ */
+#if defined(OPENSSL_FIPS)
+#include <sys/types.h>
+#include <sys/stat.h>
+#include <fcntl.h>
+#include <unistd.h>
+#include <errno.h>
+#include <stdlib.h>
+
+#define FIPS_MODE_SWITCH_FILE "/proc/sys/crypto/fips_enabled"
+#define FIPS_MODE_SWITCH_FILE2 "/etc/vmware/system_fips"
+
+static void init_fips_mode(void)
+{
+    char buf[2] = "0";
+    int fd;
+
+    if (getenv("OPENSSL_FORCE_FIPS_MODE") != NULL)
+    {
+        buf[0] = '1';
+    }
+    else if (access(FIPS_MODE_SWITCH_FILE2, F_OK) != -1)
+    {
+        buf[0] = '1';
+    }
+    else if ((fd = open(FIPS_MODE_SWITCH_FILE, O_RDONLY)) >= 0)
+    {
+        while (read(fd, buf, sizeof(buf)) < 0 && errno == EINTR);
+        close(fd);
+    }
+    /* Failure reading the fips mode switch file means just not
+     * switching into FIPS mode. We would break too many things
+     * otherwise.
+     */
+
+    if (buf[0] == '1')
+    {
+        FIPS_mode_set(1);
+    }
+}
+#endif
+
+/*
  * Perform any essential OpenSSL initialization operations. Currently only
  * sets FIPS callbacks
  */
@@ -83,7 +129,8 @@ void OPENSSL_init(void)
 # endif
     FIPS_set_error_callbacks(ERR_put_error, ERR_add_error_vdata);
     FIPS_set_malloc_callbacks(CRYPTO_malloc, CRYPTO_free);
-    RAND_init_fips();
+    RAND_get_rand_method();
+    init_fips_mode(); /* VMware patch -- check a system file */
 #endif
 #if 0
     fprintf(stderr, "Called OPENSSL_init\n");
diff -rupN vmware-OpenSSL_1_0_2v-orig/crypto/rand/rand_lib.c vmware-OpenSSL_1_0_2v-patched/crypto/rand/rand_lib.c
--- vmware-OpenSSL_1_0_2v-orig/crypto/rand/rand_lib.c	2020-05-05 14:06:00.000000000 +0000
+++ vmware-OpenSSL_1_0_2v-patched/crypto/rand/rand_lib.c	2020-07-29 19:22:59.686775706 +0000
@@ -105,7 +105,19 @@ const RAND_METHOD *RAND_get_rand_method(
             funct_ref = e;
         else
 #endif
-            default_RAND_meth = RAND_SSLeay();
+#ifdef OPENSSL_FIPS
+		/*
+		 * OpenSSL's default RNG is poor; use FIPS if available.
+		 * NB: RAND_init_fips internally calls RAND_get_rand_method,
+		 * and RAND_poll needs a sane RAND,
+		 * so this unintuitive ordering is necessary.
+		 */
+		default_RAND_meth = RAND_SSLeay();
+		RAND_init_fips();
+		default_RAND_meth = FIPS_rand_get_method();
+#else
+		default_RAND_meth = RAND_SSLeay();
+#endif
     }
     return default_RAND_meth;
 }
